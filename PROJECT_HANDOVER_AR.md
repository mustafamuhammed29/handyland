# دليل تسليم مشروع HandyLand (Handover Guide)

هذا المستند مخصص للمطور أو الفريق التقني الذي سيتولى تطوير، صيانة، أو نشر مشروع **HandyLand**. يشرح هذا الدليل هيكلية المشروع الحديثة بعد ترحيله بالكامل إلى بيئة سحابية، التقنيات المستخدمة، ومنطق العمل الأساسي من الألف إلى الياء.

---

## 1. نظرة عامة على المشروع
**HandyLand** هي منصة تجارة إلكترونية متكاملة وإدارة خدمات صيانة للهواتف والأجهزة الذكية (موجهة للسوق الألماني بشكل أساسي). تتيح للمستخدمين شراء الأجهزة والملحقات، وحجز خدمات الصيانة، وتقييم أجهزتهم القديمة لبيعها للمتجر (Ankauf)، وتضم لوحة تحكم إدارية شاملة للمسؤولين.

---

## 2. مكدس التقنيات الحديث (Tech Stack)

### **الخلفية وقاعدة البيانات (Backend & Database)**
*   **اللغة والبيئة:** Node.js مع Express.js.
*   **قاعدة البيانات والخدمات السحابية:** **Supabase Cloud**
    *   **PostgreSQL:** قاعدة البيانات العلائقية الرئيسية لإدارة المستخدمين، الطلبات، المخزون، والأسعار بأمان ومرونة عالية.
    *   **Supabase Auth:** لإدارة التسجيل، تسجيل الدخول، وحماية الجلسات باستخدام الـ JWT (بديل للمصادقة التقليدية).
    *   **Supabase Storage:** مستودع سحابي لحفظ صور المنتجات المرفوعة من لوحة التحكم وصور الهواتف.
*   **الأمان:**
    *   حماية معززة ضد هجمات الـ XSS وحقن البرمجيات الخبيثة.
    *   `helmet` و `express-rate-limit` للحماية من هجمات DDoS والطلبات المتكررة.
    *   تفعيل ميزات حماية ملفات تعريف الارتباط الآمنة (Secure, HttpOnly Cookies).
*   **الخدمات الخارجية:**
    *   `Stripe` و `PayPal` لبوابات الدفع الإلكتروني.
    *   `Nodemailer` لإرسال البريد الإلكتروني المؤتمت (مثل الفواتير وإشعارات تغيير حالة الطلب).
    *   `Socket.io` للتواصل الفوري وإشعارات المسؤولين الفورية.
    *   `eBay API` لربط ومزامنة أسعار الأجهزة مع الأسواق الخارجية.

### **الواجهة الأمامية (Frontend - User & Admin)**
*   **إطار العمل:** React.js مع **TypeScript**.
*   **أداة البناء:** Vite (لبناء برمي سريع جداً وأداء متفوق).
*   **التنسيق (Styling):** Tailwind CSS مع تصميم "Glassmorphism" وتفاعلات عصرية متميزة.
*   **الحركة (Animations):** `framer-motion` للحصول على تجربة مستخدم سلسة وعالية الفخامة (Premium UX).
*   **إدارة الحالة:** `React Context API` مع `TanStack Query` (React Query) لإدارة كاش البيانات وسرعة الاستجابة.
*   **النماذج:** `react-hook-form` مع `zod` للتحقق من البيانات (Validation).

---

## 3. هيكلية الملفات المنظفة (Directory Structure)

المشروع خضع لعملية تنظيف شاملة من جميع ملفات التجارب المؤقتة والملفات الزائدة لضمان بيئة عمل نقية:

```text
handyland/
├── backend/                  # خادم API (Node.js)
│   ├── admin/                # تطبيق لوحة التحكم (React App منفصل بـ TypeScript)
│   ├── config/               # إعدادات الحماية، الـ Swagger، وربط Supabase
│   ├── controllers/          # منطق العمل الخاص بكل مسار (Route Logic)
│   ├── models/               # تعريف الموديلات والـ Schemas لقاعدة البيانات
│   ├── routes/               # تعريف نهايات الـ API (Endpoints)
│   ├── middleware/           # برمجيات وسيطة (Auth, Error Handling, Rate Limiter)
│   ├── utils/                # أدوات مساعدة (إرسال إيميلات، معالجة صور، Socket)
│   ├── scripts/              # سكربتات الصيانة والفحص واستيراد أسعار الأجهزة
│   └── server.js             # نقطة الدخول الرئيسية للخادم
│
├── front-end/                # تطبيق الموقع الموجه للمستخدم (React App)
│   ├── src/
│   │   ├── components/       # المكونات القابلة لإعادة الاستخدام
│   │   ├── context/          # إدارة الحالة العالمية (Settings, Auth)
│   │   ├── pages/            # الصفحات الرئيسية للموقع
│   │   └── hooks/            # الدوال المخصصة (Custom Hooks)
│
└── supabase/                 # ملفات الهجرة والبذور الخاصة ببيئة Supabase
```

---

## 4. المفاهيم الأساسية والتحسينات المنجزة

### **أولاً: الانتقال الكامل إلى Supabase**
تم استبدال نظام MongoDB بالكامل بنظام قاعدة بيانات Postgres وقنوات التسجيل التابعة لـ Supabase. تم نقل إدارة المستخدمين إلى Supabase Auth لضمان أمان فائق وسرعة التحقق من الهوية.

### **ثانياً: إصلاح بناء لوحة التحكم (TS Build Fixed)**
تم حل مشكلة تجميعية هامة في لوحة التحكم الإدارية تتعلق بمتغيرات TypeScript غير المستخدمة في صفحة `ValuationManager.tsx` والتي كانت تعيق بناء المشروع للإنتاج. الآن تبنى لوحة التحكم بنسبة 100% بنجاح ودون أي تحذيرات.

### **ثالثاً: نظام إعدادات مرن ومترجم**
يتم استهلاك جميع إعدادات الموقع ونصوص الـ SEO وروابط التواصل بشكل ديناميكي من لوحة التحكم، بالإضافة إلى الترجمة الديناميكية الفائقة (الألمانية، العربية، الإنجليزية، إلخ) عبر `i18next`.

---

## 5. كيفية التشغيل المحلي والتأكد من الجاهزية (Local Setup & Verification)

لتشغيل الخدمات الثلاث معاً في ثوانٍ معدودة، استخدم السكريبت المرفق في الجذر:
```bash
# تشغيل كل الخدمات في نوافذ منفصلة تلقائياً
start.bat
```

### **التأكد الفردي من جاهزية البناء:**

1. **الواجهة الأمامية (Frontend):**
   ```bash
   cd front-end
   npm run build  # للتأكد من نجاح توليد ملفات الإنتاج والـ PWA
   ```

2. **لوحة التحكم (Admin):**
   ```bash
   cd backend/admin
   npm run build  # يبني بنجاح 100% بدون أي أخطاء تجميع
   ```

3. **الاختبارات الخلفية (Backend Tests):**
   ```bash
   cd backend
   npm run test   # لتشغيل اختبارات الوحدات والتأكد من نجاحها بالكامل
   ```

---

## 6. ملاحظات هامة للمرحلة النهائية
* **الملفات الزائدة:** تم تنظيف المشروع بالكامل من ملفات `.log` الزائدة، وصور التجارب، وملفات سكراب الأسعار المؤقتة (`bm.html` إلخ) ليكون حجم المستودع خفيفاً.
* **الأمان في الإنتاج:** تأكد من إعداد بوابات الدفع Stripe ومفتاح التشفير وخدمة البريد SMTP داخل ملف الـ `.env` المرفق مع السيرفر في بيئة الاستضافة الفعلية كما هو مشروح بالتفصيل في ملف `DEPLOYMENT.md`.
