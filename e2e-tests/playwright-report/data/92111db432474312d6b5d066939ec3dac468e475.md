# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: frontend\auth.spec.ts >> Frontend Authentication Flow >> User can navigate to register and see form
- Location: tests\frontend\auth.spec.ts:29:7

# Error details

```
Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:5173/register
Call log:
  - navigating to "http://localhost:5173/register", waiting until "load"

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Frontend Authentication Flow', () => {
  4  |   test('User can navigate to login and see form', async ({ page }) => {
  5  |     await page.goto('/login');
  6  |     await expect(page).toHaveTitle(/HandyLand/i);
  7  |     
  8  |     // Check elements using placeholders or labels
  9  |     await expect(page.locator('input[type="email"]')).toBeVisible();
  10 |     await expect(page.locator('input[type="password"]')).toBeVisible();
  11 |     await expect(page.locator('button[type="submit"]')).toBeVisible();
  12 |   });
  13 | 
  14 |   test('Login shows error on invalid credentials', async ({ page }) => {
  15 |     await page.goto('/login');
  16 |     
  17 |     // Fill form
  18 |     await page.fill('input[type="email"]', 'wrong@example.com');
  19 |     await page.fill('input[type="password"]', 'WrongPass123!');
  20 |     
  21 |     // Submit
  22 |     await page.click('button[type="submit"]');
  23 |     
  24 |     // Check error message (wait for it to appear)
  25 |     const errorAlert = page.locator('.bg-red-500\\/10');
  26 |     await expect(errorAlert).toBeVisible({ timeout: 5000 });
  27 |   });
  28 | 
  29 |   test('User can navigate to register and see form', async ({ page }) => {
> 30 |     await page.goto('/register');
     |                ^ Error: page.goto: net::ERR_CONNECTION_REFUSED at http://localhost:5173/register
  31 |     
  32 |     await expect(page.locator('input[name="name"]')).toBeVisible();
  33 |     await expect(page.locator('input[name="email"]')).toBeVisible();
  34 |     await expect(page.locator('input[name="password"]')).toBeVisible();
  35 |     await expect(page.locator('input[name="confirmPassword"]')).toBeVisible();
  36 |     
  37 |     // Phone input from react-phone-number-input usually has type="tel"
  38 |     await expect(page.locator('input[type="tel"]')).toBeVisible();
  39 |   });
  40 | 
  41 |   test('Registration form validates required fields', async ({ page }) => {
  42 |     await page.goto('/register');
  43 |     
  44 |     // Submit empty
  45 |     await page.click('button[type="submit"]');
  46 |     
  47 |     // HTML5 validation or custom validation error should appear
  48 |     const errorAlert = page.locator('.bg-red-500\\/10');
  49 |     await expect(errorAlert).toBeVisible();
  50 |   });
  51 |   
  52 |   test('Registration validates password mismatch', async ({ page }) => {
  53 |     await page.goto('/register');
  54 |     
  55 |     await page.fill('input[name="name"]', 'Test User');
  56 |     await page.fill('input[name="email"]', 'test_new@example.com');
  57 |     await page.fill('input[type="tel"]', '+4915123456789');
  58 |     
  59 |     // Passwords mismatch
  60 |     await page.fill('input[name="password"]', 'Password123!');
  61 |     await page.fill('input[name="confirmPassword"]', 'Password123!456');
  62 |     
  63 |     await page.click('button[type="submit"]');
  64 |     
  65 |     // Should show error message
  66 |     const errorAlert = page.locator('.bg-red-500\\/10');
  67 |     await expect(errorAlert).toBeVisible();
  68 |     await expect(errorAlert).toContainText(/stimmen nicht überein|not match/i);
  69 |   });
  70 | });
  71 | 
```